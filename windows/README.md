# Windows

This folder is reserved for native Windows bridge builds.

## Expected artifact

- `runloom-bridge.exe`

## Status

The Windows app is not built from this macOS machine. Produce it on a native Windows runner, then place the generated executable or installer in this folder.
