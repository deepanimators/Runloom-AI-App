# Linux

This folder is reserved for native Linux bridge builds.

## Expected artifact

- `runloom-bridge`

## Status

The Linux app is not built from this macOS machine. Produce it on a native Linux runner, then place the generated binary or installer package in this folder.
